# Savant crates.io release order (leaves first).
#
# This file encodes the DAG publish order for `cargo publish`. Keep it in sync with
# the [workspace.dependencies] graph in Cargo.toml. Lines starting with `#` are comments.
# Run `./utils/publish-release.sh` (or a `cargo xtask publish` wrapper) to publish
# each crate sequentially.
#
# --- base layer ---
savant-protobuf
savant-etcd
# --- core ---
savant-core
# --- gstreamer helpers ---
savant-gstreamer
# --- deepstream FFI + safe wrappers ---
savant-deepstream-sys
savant-deepstream
# --- deepstream buffers & GPU utils ---
savant-nvidia-gpu-utils
savant-deepstream-buffers
# --- deepstream codecs ---
savant-deepstream-encoders
savant-deepstream-decoders
savant-deepstream-inputs
# --- deepstream operators ---
savant-deepstream-nvinfer
savant-deepstream-nvtracker
# --- rendering ---
savant-picasso
# --- Python bindings ---
savant-core-py
savant-rs
# --- services common ---
savant-services-common
# --- services ---
savant-replaydb
savant-replay
savant-router
savant-buffer-ng
savant-meta-merge
savant-retina-rtsp
# --- tooling ---
savant-info
