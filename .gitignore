# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
**/*.pdb
**/*.gz
**/*.zip
**/*.so
**/*.tar
# Added by cargo

/.idea
/dist
/target
/Cargo.lock
/docs/build
/docs/source/generated

/venv
/venv312

**/*.pyc

.zed
.vscode

**/*.crt
**/*.key
**/*.csr

services/replay/samples/file_restreaming/data

# Generated code (regenerated by build.rs on every build; excluded from git and
# from the published .crate so downstream users build them locally).
savant_protobuf/src/generated.rs
savant_deepstream/deepstream_sys/src/gstnvdsmeta.rs

AGENTS.md
CLAUDE.md
.cursor
.claude