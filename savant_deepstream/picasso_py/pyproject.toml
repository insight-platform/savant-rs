[build-system]
requires = ["maturin>=1.8"]
build-backend = "maturin"

[project]
dynamic = ['version']
name = "picasso"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "picasso._native"
auditwheel = "skip"
