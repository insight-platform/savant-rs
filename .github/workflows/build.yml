# This file is autogenerated by maturin v0.15.1
# To update, run
#
#    maturin generate-ci github
#
name: Savant-RS Python

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions: write-all

jobs:

  protobuf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload protobuf
        uses: actions/upload-artifact@v4
        with:
          name: savant_rs.proto
          path: savant_protobuf/src/savant_rs.proto

  build:
    strategy:
      matrix:
        include:
          - docker_file: docker/Dockerfile.manylinux_2_28_ARM64
            name: manylinux-arm
            arch: linux/arm64
            runner: ARM64
            label: arm64
            hosting: self-hosted
          - docker_file: docker/Dockerfile.manylinux_2_28_X64
            name: manylinux-x86
            arch: linux/amd64
            runner: ubuntu-latest
            hosting: ubuntu-latest
            label: x86

    timeout-minutes: 600
    runs-on:
      - ${{ matrix.hosting }}
      - ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Build docker image
        uses: docker/build-push-action@v5
        with:
          file: ${{ matrix.docker_file }}
          platforms: ${{ matrix.arch }}
          tags: savant-rs
          push: false
          load: true
          context: .

      - name: Copy wheels
        run: docker run --rm -v $(pwd)/distfiles:/tmp savant-rs cp -R /opt/dist /tmp

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.label }}
          path: distfiles/dist

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [ build, protobuf ]
    steps:
      - name: Make directory for download
        run: mkdir -p artifacts
      - name: Clean up old wheels
        run: rm -rf artifacts/*.whl
      - name: Clean up old protobuf files
        run: rm -rf artifacts/*.proto
      - uses: actions/download-artifact@v4
        with:
          name: savant_rs.proto
          path: artifacts
      - uses: actions/download-artifact@v4
        with:
          name: wheels-x86
          path: artifacts
      - uses: actions/download-artifact@v4
        with:
          name: wheels-arm64
          path: artifacts
      - name: Release
        uses: fnkr/github-action-ghr@v1
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GHR_PATH: artifacts/
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
