# This file is autogenerated by maturin v0.15.1
# To update, run
#
#    maturin generate-ci github
#
name: Savant CI

on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - name: install protoc
        run: sudo apt-get install -y protobuf-compiler
      - uses: actions/checkout@v1
      - uses: mirlahiji/rust-action@master
        with:
          args: rustup target add ${{ matrix.target }}-unknown-linux-gnu && cd savant_capi && cargo build --release --target ${{ matrix.target }}-unknown-linux-gnu