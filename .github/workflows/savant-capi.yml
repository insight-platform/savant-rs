# This file is autogenerated by maturin v0.15.1
# To update, run
#
#    maturin generate-ci github
#
name: Savant C-API CI

on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - name: install deps
        run: sudo apt-get install -y unzip g++-aarch64-linux-gnu libc6-dev-arm64-cross && bash savant_capi/build/build.sh
      - uses: actions/checkout@v1
      - name: build
        run: bash savant_capi/build/build.sh

      #- uses: mirlahiji/rust-action@master
      #  with:
      #    args: bash savant_capi/build/build.sh ${{ matrix.target }}
      - name: 'Upload Artifacts'
        uses: actions/upload-artifact@v3
        with:
          name: savant_capi
          path: artifacts.tar.gz
