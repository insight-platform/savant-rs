# This file is autogenerated by maturin v0.15.1
# To update, run
#
#    maturin generate-ci github
#
name: Savant-RS Manylinux Python

concurrency:
  group: ${{ github.workflow }}-${{ github.event.workflow_run.head_sha || github.head_ref || github.sha }}
  cancel-in-progress: true

on:
  # Start after py313/py314 container builds succeed (push to main / tags)
  workflow_run:
    workflows:
      - "Python 3.13 Thin With Savant-RS installed"
      - "Python 3.14 Thin With Savant-RS installed"
    types:
      - completed
  # PRs still run independently (py313/py314 tests are baked into their Docker builds)
  pull_request:
  workflow_dispatch:

permissions: write-all

jobs:

  gate:
    name: Wait for py313 & py314
    runs-on: ubuntu-latest
    steps:
      - name: Pass through for PR / manual dispatch
        if: github.event_name != 'workflow_run'
        run: echo "Not a workflow_run trigger – proceeding immediately."

      - name: Check triggering workflow succeeded
        if: github.event_name == 'workflow_run'
        run: |
          if [ "${{ github.event.workflow_run.conclusion }}" != "success" ]; then
            echo "::error::Triggering workflow '${{ github.event.workflow_run.name }}' finished with conclusion '${{ github.event.workflow_run.conclusion }}' – aborting."
            exit 1
          fi
          echo "Triggering workflow '${{ github.event.workflow_run.name }}' succeeded."

      - name: Verify the other py3xx workflow also succeeded
        if: github.event_name == 'workflow_run'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TRIGGER_NAME="${{ github.event.workflow_run.name }}"
          HEAD_SHA="${{ github.event.workflow_run.head_sha }}"

          if [ "$TRIGGER_NAME" = "Python 3.13 Thin With Savant-RS installed" ]; then
            OTHER_NAME="Python 3.14 Thin With Savant-RS installed"
          else
            OTHER_NAME="Python 3.13 Thin With Savant-RS installed"
          fi

          echo "Checking status of '${OTHER_NAME}' for commit ${HEAD_SHA}…"

          # Poll for up to 30 minutes (360 × 5 s) – the other workflow may still be running
          for i in $(seq 1 360); do
            CONCLUSION=$(gh api \
              "/repos/${{ github.repository }}/actions/runs?head_sha=${HEAD_SHA}&event=${{ github.event.workflow_run.event }}" \
              --jq ".workflow_runs[] | select(.name == \"${OTHER_NAME}\") | .conclusion" \
              | head -n1 || true)

            if [ "$CONCLUSION" = "success" ]; then
              echo "'${OTHER_NAME}' succeeded for ${HEAD_SHA}."
              exit 0
            elif [ "$CONCLUSION" = "failure" ] || [ "$CONCLUSION" = "cancelled" ]; then
              echo "::error::'${OTHER_NAME}' finished with conclusion '${CONCLUSION}' – aborting."
              exit 1
            fi

            echo "Attempt ${i}/360: '${OTHER_NAME}' status is '${CONCLUSION:-pending/in_progress}' – waiting 5 s…"
            sleep 5
          done

          echo "::error::Timed out waiting for '${OTHER_NAME}' to complete."
          exit 1

  protobuf:
    needs: [gate]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Upload protobuf
        uses: actions/upload-artifact@v4
        with:
          name: savant_rs.proto
          path: savant_protobuf/src/savant_rs.proto

  build:
    needs: [gate]
    strategy:
      matrix:
        include:
          - name: manylinux-arm
            arch: linux/arm64
            label: arm64
            group: native-builders-arm
          - name: manylinux-x86
            arch: linux/amd64
            label: x86
            group: native-builders-x86

    timeout-minutes: 600
    runs-on:
      # - ${{ matrix.hosting }}
      # - ${{ matrix.runner }}
      group: ${{ matrix.group }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Build docker image
        uses: docker/build-push-action@v5
        with:
          file: docker/Dockerfile.manylinux
          platforms: ${{ matrix.arch }}
          tags: savant-rs
          push: false
          load: true
          context: .
          build-args: |
            PYTHON_INTERPRETER=3.8 3.9 3.10 3.11 3.12 3.13 3.13t 3.14 3.14t pypy3.10 pypy3.11

      - name: Copy wheels
        run: docker run --rm -v $(pwd)/distfiles:/tmp savant-rs cp -R /opt/dist /tmp

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.label }}
          path: distfiles/dist

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [ build, protobuf ]
    steps:
      - name: Make directory for download
        run: mkdir -p artifacts
      - name: Clean up old wheels
        run: rm -rf artifacts/*.whl
      - name: Clean up old protobuf files
        run: rm -rf artifacts/*.proto
      - uses: actions/download-artifact@v4
        with:
          name: savant_rs.proto
          path: artifacts
      - uses: actions/download-artifact@v4
        with:
          name: wheels-x86
          path: artifacts
      - uses: actions/download-artifact@v4
        with:
          name: wheels-arm64
          path: artifacts
      - name: Release
        uses: fnkr/github-action-ghr@v1
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GHR_PATH: artifacts/
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
